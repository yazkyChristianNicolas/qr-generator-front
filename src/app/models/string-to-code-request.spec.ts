import { StringToCodeRequest } from './string-to-code-request';

describe('StringToCodeRequest', () => {
  it('should create an instance', () => {
    expect(new StringToCodeRequest()).toBeTruthy();
  });
});
