import { StringToCodeResponse } from './string-to-code-response';

describe('StringToCodeResponse', () => {
  it('should create an instance', () => {
    expect(new StringToCodeResponse()).toBeTruthy();
  });
});
